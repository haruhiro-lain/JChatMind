# 本地数据库配置（profile 默认值）
#
# 数据库连接信息统一从 .env 环境变量中读取，此文件提供本地场景的默认值。
# 修改数据库信息只需编辑 .env，无需修改此文件。
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:127.0.0.1}:${DB_PORT:15432}/${DB_NAME:mindharness}
