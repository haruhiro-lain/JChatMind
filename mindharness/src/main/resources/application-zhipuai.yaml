# 智谱 AI 配置（可选）
# 使用方式: mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=zhipuai
spring:
  ai:
    zhipuai:
      api-key: ${ZHIPUAI_API_KEY:}
      base-url: https://open.bigmodel.cn/api/paas
      chat:
        options:
          model: glm-4.6
