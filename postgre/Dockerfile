# Simple Dockerfile that uses pgvector as base so pgvector extension is available
FROM pgvector/pgvector:pg16

# Keep default entrypoint from base image (Postgres)
# Add any additional setup here if needed
